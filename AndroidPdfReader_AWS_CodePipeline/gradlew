#!/usr/bin/env bash
#
# Copyright 2015 the original author or authors.
# ... [file content as previously provided]
#
exec "$JAVACMD" \
    "${DEFAULT_JVM_OPTS[@]}" \
    "${JAVA_OPTS[@]}" \
    "${GRADLE_OPTS[@]}" \
    "-Dorg.gradle.appname=$APP_NAME_ESCAPED" \
    -classpath "$CLASSPATH" \
    org.gradle.wrapper.GradleWrapperMain "$@"
